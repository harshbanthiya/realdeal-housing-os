Status pill (mono 11px, tint/10 bg) for verification/launch states — use for statuses, never as a tag cloud.

```jsx
<Pill tone="blocked">3 blockers · go-live locked</Pill>
```

Tones: `ready` (teal) · `blocked` (warm) · `review` (amber) · `active` (accent blue) · `neutral` (mist).
