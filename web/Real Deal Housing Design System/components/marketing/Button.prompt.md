Pill CTA link-button. Hover: solid → opacity .9; outline → mist fill. Arrows in label: `View listings →`.

```jsx
<Button href="/buy">View listings →</Button>
<Button variant="outline">See verified facts</Button>
<Button variant="warm">WhatsApp Padmini</Button>
```

Rule: max one `warm` element per viewport.
