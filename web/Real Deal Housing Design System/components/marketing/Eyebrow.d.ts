/** Numbered section eyebrow ("02 — Project overview") — the scroll-story wayfinding device. */
export interface EyebrowProps {
  /** two-digit section numeral, e.g. "04" */
  n?: string;
  label: string;
  /** render on a teal chapter section */
  onDark?: boolean;
}
