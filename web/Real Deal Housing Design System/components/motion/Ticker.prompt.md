Infinite marquee strip (building names, areas). Decorative — max one per page, mid-dot separators in warm.

```jsx
<Ticker items={["Imperial Heights", "Kalpataru Radiance", "Ekta Tripolis", "DLF Westpark"]}
  itemStyle={{ fontSize: 40, fontWeight: 800, letterSpacing: "-0.02em", color: "var(--teal)" }} />
```
