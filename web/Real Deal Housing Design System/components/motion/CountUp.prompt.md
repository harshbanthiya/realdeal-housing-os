Count-up stat numeral (tabular nums, expo ease). Verification-first rule applies: only animate real numbers.

```jsx
<CountUp value={15} suffix=" yrs" style={{ fontSize: 56, fontWeight: 800, color: "var(--teal)" }} />
```
